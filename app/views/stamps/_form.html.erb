<%= form_for(@stamps) do |f| %>

  <% if @stamp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stamp.errors.count, "error") %> prohibited this stamp from being saved:</h2>

      <ul>
      <% @stamp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%#= date_select :search, params[:search], discard_day: true, start_year: Date.today.year, end_year: 2009, prompt: { month: 'Choose month' } %>
  
  <div>
    <%#= submit_tag "Search" %>
  </div>
  

<% end %>
